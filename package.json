{
  "name": "@workspace/root",
  "version": "1.0.0",
  "private": true,
  "description": "Tickety V2 monorepo",
  "scripts": {
    "build": "pnpm --filter @workspace/dashboard run build",
    "dev": "pnpm --filter @workspace/dashboard run dev",
    "start": "pnpm --filter @workspace/dashboard run start",
    "bot:dev": "pnpm --filter @workspace/discord-bot run dev",
    "bot:start": "pnpm --filter @workspace/discord-bot run start"
  },
  "devDependencies": {
    "pnpm": "^9.0.0"
  }
}
